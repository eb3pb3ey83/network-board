import { styled } from '@mui/material';

export const HeaderWrapper = styled('div')({
  background: 'rgba(116, 213, 253, 0.40)',
  padding: '10px 0',
});
export const HeaderContainer = styled('div')({
  margin: 'auto',
  width: '2',
});

export const Header = styled('div')({
  display: 'flex',
  alignItems: 'center',
  gap: '10px',
  fontWeight: 600,
});
