import { styled } from '@mui/material';
export const TagContainer = styled('div')(() => ({
  display: 'flex',
  flexWrap: 'wrap',
  gap: '6px',
  padding: '12px 16px',
}));
export const Tag = styled('div')(() => ({
  background: '#FAF1F2',
  color: '#DD060E',
  padding: '4px 10px',
  borderRadius: '8px',
  fontSize: '16px',
}));
