export type SelectOption = {
  label: string;
  value: string;
};

export type RadioOptions = {
  label: string;
  value: string | number;
};

export type SizeBreakPoint = 'small' | 'medium' | 'large';

export type ModalMode = 'add' | 'edit' | 'view';

export type Order = 'asc' | 'desc';
