import { styled } from '@mui/material';

export const CsvDownloadWrapper = styled('div')({
  display: 'grid',
  gridTemplateColumns: 'repeat(2, 1fr)',
  columnGap: '12px',
  rowGap: '12px',
  paddingTop: '16px',
});
